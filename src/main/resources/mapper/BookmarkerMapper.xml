
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.HowAboutThisWay.mapper.BookmarkerMapper">

    <!-- 1) 내 즐겨찾기 목록 조회 -->
    <select id="findByUserId"
            parameterType="string"
            resultType="com.HowAboutThisWay.dto.BookmarkerDTO">
        SELECT
            idx,
            address,
            label
        FROM bookmarker
        WHERE userId = #{userId}
    </select>

    <!-- 2) 내 즐겨찾기 등록 -->
    <insert id="insertBookmarker"
            parameterType="com.HowAboutThisWay.dto.BookmarkerDTO"
            useGeneratedKeys="true"
            keyProperty="idx">
        INSERT INTO bookmarker
            (userId, address, label, createdAt, updatedAt)
        VALUES
            (#{userId}, #{address}, #{label}, #{createdAt}, #{updatedAt})
    </insert>

    <!-- 3) 내 즐겨찾기 수정 -->
    <update id="updateBookmarker"
            parameterType="com.HowAboutThisWay.dto.BookmarkerDTO">
        UPDATE bookmarker
        SET address   = #{address},
        label     = #{label},
        updatedAt = #{updatedAt}
        WHERE idx    = #{idx}
        AND userId = #{userId}
    </update>

    <!-- 4) 내 즐겨찾기 삭제 -->
    <delete id="deleteBookmarker"
            parameterType="map">
        DELETE FROM bookmarker
        WHERE idx    = #{idx}
          AND userId = #{userId}
    </delete>

</mapper>
